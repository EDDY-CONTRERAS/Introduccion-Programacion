#include <stdio.h>

/* Aplicaciones de operadores. */

void main (void)
{
int i = 5, j = 4, k, 1, m;
k = !i * 3 + --j * 2 - 3;
printf ("\nEl valor de k es: %d", k);

1 = ! (!i ║ 1 && Ø) && 1;
printf ("\nEl valor de 1 es: %d", 1);

m = (k = (! (12 > 1Ø)), j = (1Ø ║ Ø) && k, (! (k ║ j)));
printf ("\nEl valor de m es: %d", m);
}
